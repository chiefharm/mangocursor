# Инструкции для AI-агента

1. Прочитай **`PROJECT_MEMORY.md`** — там полный контекст проекта.
2. Не коммить `.env` и токены Telegram.
3. Для отправки в Telegram на iPhone используй **DOCX** (`daily_pipeline.py` или `python-docx`).
4. Деплой на VPS: `deploy/vps_install.sh` → `/opt/mango-pipeline`.
